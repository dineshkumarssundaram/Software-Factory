server:
  port: 8081
spring:
  application:
    name: user-service
  profiles:
    active: test
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    discovery:
      enabled: true
    config:
      enabled: true