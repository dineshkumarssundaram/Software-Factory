server:
  port: 8083
spring:
  application:
    name: orchestrator-service
  profiles:
    active: dev
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    discovery:
      enabled: true
    config:
      enabled: true