server:
  port: 8082
spring:
  application:
    name: project-service
  profiles:
    active: test
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    discovery:
      enabled: true
    config:
      enabled: true