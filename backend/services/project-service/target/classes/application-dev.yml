server:
  port: 8082
spring:
  application:
    name: project-service
  profiles:
    active: dev
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    discovery:
      enabled: true
    config:
      enabled: true